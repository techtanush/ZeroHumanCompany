# @zeroth/simpop

TypeScript implementation of the synthetic population service. The architecture note calls for Rust + axum, but this package intentionally uses TypeScript so it can share the existing contracts and run without an extra toolchain.
